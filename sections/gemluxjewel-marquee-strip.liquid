{{ 'gemluxjewel-marquee.css' | asset_url | stylesheet_tag }}
{{ 'section-gemluxjewel-home.css' | asset_url | stylesheet_tag }}

<section class="gemluxjewel gemluxjewel-marquee-strip">
  {%- capture strip_marquee_content -%}
    {%- for block in section.blocks -%}
      <div class="gemluxjewel-marquee-strip__item" {{ block.shopify_attributes }}>
        {{ block.settings.text | escape }}
        <span class="gemluxjewel-marquee-strip__star">
          <img src="{{ 'star.svg' | asset_url }}" alt="" width="50" height="50">
        </span>
      </div>
    {%- endfor -%}
  {%- endcapture -%}
  {% render 'gemluxjewel-marquee', class: 'gemluxjewel-marquee-strip__inner', duration: section.settings.speed, gap: 0, content: strip_marquee_content %}
</section>

{% schema %}
{
  "name": "gemluxjewel Marquee Strip",
  "tag": "section",
  "class": "section gemluxjewel-section",
  "settings": [
    {
      "type": "range",
      "id": "speed",
      "min": 15,
      "max": 60,
      "step": 5,
      "unit": "s",
      "label": "Scroll speed",
      "default": 25
    }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Text",
      "settings": [{ "type": "text", "id": "text", "label": "Text", "default": "Timeless Style" }]
    }
  ],
  "presets": [
    {
      "name": "gemluxjewel Marquee Strip",
      "blocks": [
        { "type": "text", "settings": { "text": "Timeless Style" } },
        { "type": "text", "settings": { "text": "Cold Shield" } },
        { "type": "text", "settings": { "text": "Smart Insulation" } },
        { "type": "text", "settings": { "text": "Winter Comfort" } }
      ]
    }
  ]
}
{% endschema %}
