{{ 'section-dirona-home.css' | asset_url | stylesheet_tag }}
{% render 'dirona-fonts' %}
<script src="{{ 'dirona-home.js' | asset_url }}" defer="defer"></script>

<section class="dirona dirona-hero" data-dirona-hero data-autoplay="{{ section.settings.autoplay_speed }}">
  <div class="dirona-hero__layer"></div>
  <div class="dirona-hero__slides">
    {%- if section.blocks.size > 0 -%}
      {%- for block in section.blocks -%}
        <div class="dirona-hero__slide{% if forloop.first %} is-active{% endif %}" data-dirona-hero-slide {{ block.shopify_attributes }}>
          {%- if block.settings.image != blank -%}
            {{ block.settings.image | image_url: width: 3840 | image_tag: widths: '750, 1100, 1500, 2000, 3840', sizes: '100vw', fetchpriority: forloop.first }}
          {%- else -%}
            <img src="{{ 'hero-img.png' | asset_url }}" alt="" width="1920" height="900" loading="{% if forloop.first %}eager{% else %}lazy{% endif %}">
          {%- endif -%}
        </div>
      {%- endfor -%}
    {%- elsif section.settings.image != blank -%}
      <div class="dirona-hero__slide is-active" data-dirona-hero-slide>
        {{ section.settings.image | image_url: width: 3840 | image_tag: widths: '750, 1100, 1500, 2000, 3840', sizes: '100vw', fetchpriority: 'high' }}
      </div>
    {%- else -%}
      <div class="dirona-hero__slide is-active" data-dirona-hero-slide>
        <img src="{{ 'hero-img.png' | asset_url }}" alt="" width="1920" height="900" fetchpriority="high">
      </div>
    {%- endif -%}
  </div>

  <div class="dirona-hero__content">
    <div class="dirona-container">
      {%- if section.settings.prefix != blank -%}
        <p class="dirona-prefix">{{ section.settings.prefix }}</p>
      {%- endif -%}
      {%- if section.settings.title != blank -%}
        <h1 class="dirona-hero__title">{{ section.settings.title }}</h1>
      {%- endif -%}
      {%- if section.settings.text != blank -%}
        <p class="dirona-hero__text">{{ section.settings.text }}</p>
      {%- endif -%}
      {% render 'dirona-btn', label: section.settings.button_label, url: section.settings.button_link, style: 'green' %}
    </div>
  </div>

  <div class="dirona-hero__float">
    <div class="dirona-container">
      <div class="dirona-hero__float-inner">
        <div class="dirona-hero__product-box">
          <div class="dirona-hero__product-thumb">
            {%- assign preview_product = section.settings.preview_product -%}
            {%- if preview_product != blank and preview_product.featured_media -%}
              <img src="{{ preview_product.featured_media | image_url: width: 200 }}" alt="{{ preview_product.title | escape }}" data-dirona-hero-preview width="100" height="80">
            {%- elsif section.blocks.first.settings.image != blank -%}
              <img src="{{ section.blocks.first.settings.image | image_url: width: 200 }}" alt="" data-dirona-hero-preview width="100" height="80">
            {%- else -%}
              <img src="{{ 'hero-product-img.png' | asset_url }}" alt="" data-dirona-hero-preview width="100" height="80">
            {%- endif -%}
          </div>
          <div style="flex:1">
            <p class="dirona-hero__product-label">{{ section.settings.preview_label | default: section.settings.prefix }}</p>
            <p class="dirona-hero__product-desc">{{ section.settings.preview_text | default: section.settings.title }}</p>
            <div class="dirona-hero__progress"><div class="dirona-hero__progress-fill" data-dirona-hero-progress></div></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Dirona Hero",
  "tag": "section",
  "class": "section dirona-section",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Fallback slide image" },
    { "type": "range", "id": "autoplay_speed", "min": 3000, "max": 8000, "step": 500, "default": 5000, "unit": "ms", "label": "Slide speed" },
    { "type": "text", "id": "prefix", "label": "Prefix", "default": "Hypoallergenic Titanium Jewellery" },
    { "type": "text", "id": "title", "label": "Heading", "default": "Gentle on Your Skin. Perfect for Daily Wear." },
    { "type": "textarea", "id": "text", "label": "Description", "default": "Expert-crafted titanium jewellery that's hypoallergenic, waterproof, and incredibly lightweight." },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "View Collection" },
    { "type": "url", "id": "button_link", "label": "Button link" },
    { "type": "product", "id": "preview_product", "label": "Preview product" },
    { "type": "text", "id": "preview_label", "label": "Preview box label" },
    { "type": "text", "id": "preview_text", "label": "Preview box text" }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [{ "type": "image_picker", "id": "image", "label": "Image" }]
    }
  ],
  "presets": [{ "name": "Dirona Hero", "blocks": [{ "type": "slide" }, { "type": "slide" }, { "type": "slide" }] }]
}
{% endschema %}
