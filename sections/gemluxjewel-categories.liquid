{{ 'section-dirona-home.css' | asset_url | stylesheet_tag }}

<section class="dirona dirona-categories">
  <div class="dirona-container">
    <div class="dirona-categories__scroll">
      {%- for block in section.blocks -%}
        <a href="{{ block.settings.link }}" class="dirona-categories__item" {{ block.shopify_attributes }}>
          <div class="dirona-categories__img">
            {%- if block.settings.image != blank -%}
              {{ block.settings.image | image_url: width: 400 | image_tag: widths: '155, 200, 400', sizes: '200px', loading: 'lazy' }}
            {%- else -%}
              <img src="{{ 'category-img.png' | asset_url }}" alt="{{ block.settings.title | escape }}" width="200" height="200" loading="lazy">
            {%- endif -%}
          </div>
          <div class="dirona-categories__title">{{ block.settings.title | escape }}</div>
          <span class="dirona-categories__link">Explore</span>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Dirona Categories",
  "tag": "section",
  "class": "section dirona-section",
  "max_blocks": 8,
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Image" },
        { "type": "text", "id": "title", "label": "Title", "default": "Earrings" },
        { "type": "url", "id": "link", "label": "Link" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Dirona Categories",
      "blocks": [
        { "type": "category", "settings": { "title": "Earrings" } },
        { "type": "category", "settings": { "title": "Cartilage Hoops" } },
        { "type": "category", "settings": { "title": "Flat Back Studs" } },
        { "type": "category", "settings": { "title": "Huggies" } },
        { "type": "category", "settings": { "title": "Stud Earrings" } },
        { "type": "category", "settings": { "title": "Hoops" } }
      ]
    }
  ]
}
{% endschema %}
